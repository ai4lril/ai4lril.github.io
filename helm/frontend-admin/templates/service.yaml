apiVersion: v1
kind: Service
metadata:
  name: {{ include "frontend-admin.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "frontend-admin.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    {{- include "frontend-admin.selectorLabels" . | nindent 4 }}
